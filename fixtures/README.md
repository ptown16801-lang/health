# Private fixture placement

Do not put real fixtures in this tracked directory. Store protected `.one`/`.onepkg`
files under `.private/fixtures/` (ignored by Git) on a private local volume. See
`docs/JON-107-validation.md` for permissions and invocation.
