# Private reference placement

Do not put real screenshots, exports, expected-content notes, or source inventories
in this tracked directory. Store them under `.private/references/` (ignored by Git)
on a private local volume.
